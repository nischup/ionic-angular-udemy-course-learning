import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-ionic-learning',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
